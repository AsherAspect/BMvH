# BMvH
BMvH projects

## Streamlit instructions
- Streamlit installation: `uv pip install --upgrade pip setuptools wheel; uv pip install --only-binary=:all: streamlit pyarrow`
- Execution:
    - Open a powershell inside of the root dir
    - Run `python -m streamlit run app.py`